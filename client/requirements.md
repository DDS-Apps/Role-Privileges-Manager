## Packages
framer-motion | For smooth transitions between view/edit modes and page animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
The app requires a mock data initialization on the backend if the JSON files don't exist yet.
Language toggle (EN/AR) requires setting the `dir` attribute on the root element.
